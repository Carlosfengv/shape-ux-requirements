interface:
  display_name: "UX 需求梳理"
  short_description: "面向 UX 与 PM 的需求梳理、ASCII UI 与规格"
  default_prompt: "请使用 $shape-ux-requirements 先分析仓库、需求证据、背景、用户、场景、范围和 user stories，输出需求确认稿并等待我确认；确认后再生成完整规格、可追踪的 ASCII UI 和支持文件。"

policy:
  allow_implicit_invocation: false
